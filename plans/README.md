# Implementation Plans

- 001~007: improve 스킬 2026-08-09 생성 (기준 커밋 `b63da64`) — **전부 완료**
- 008~011: improve 스킬 2026-08-16 생성 (기준 커밋 `9031f65`)

각 실행자는 **계획 파일이 있으면 끝까지 읽고** STOP 조건을 준수하며, 완료 시 자기 행의 Status 를 갱신하라.
⚠ **012 이후에는 계획 파일이 없다**(감사 #40) — 감사 후속 대응이라 계획서 없이 바로 실행했다.
그 항목들의 근거는 **커밋 메시지**에 상세히 남아 있으니 `git log --grep` 으로 찾아 그것을 근거로 삼아라.
⚠ 이 레포의 운영 절대 룰: **공유 production DB 직접 수정 금지**(운영자 명시 승인 후만)·
**push 는 운영자 승인 후**.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | 실거래 DB 조회 1000행 페이징 (고거래 월 최대 47% 누락 실측) | P1 | S | — | DONE (4983974, 2026-08-09) |
| 002 | 공개 geocode 라우트 일일 총량 캡 (Kakao 쿼터 고갈 증폭 차단) | P1 | M | — | DONE (0e0f982, 2026-08-09) |
| 003 | AI 비용 카운터·좌표 저장 await (서버리스 동결 유실 차단) | P1 | S | — | DONE (5e6190a, 2026-08-09) |
| 004 | 결제 이월·규제 판정 계약 테스트 (실사고 이력 경로 고정) | P1 | S~M | — | DONE (d50ed75, 2026-08-09) |
| 005 | README·.env.example·engines 실동작 정합 | P2 | S | — | DONE (b91fda1, 2026-08-09) |
| 006 | MOLIT 파싱 공통 헬퍼 (5곳 복붙 제거 + 숫자금액 TypeError 종결) | P1 | S~M | — | DONE (d72a35e, 2026-08-09) |
| 007 | Supabase 클라이언트 SSOT (24파일 자체 createClient 제거) | P1 | M | 006 먼저 커밋(파일 겹침) | DONE (acfd032, 2026-08-09) |
| **008** | **프론트 취득세 6억 경계 '이하' 정정 + 프론트↔백엔드 계약 테스트** | **P1** | S | — | DONE (02f4a26, 2026-08-16) |
| **009** | **`molit_transactions.deal_date` 인덱스 (2.2초 조회 제거)** | **P1** | S | — | DONE (DDL only, 2026-08-16) |
| **010** | **프론트 정적 결함 2건 (XSS 싱크 escape, 중복 키로 죽은 타임아웃)** | **P1** | S | — | DONE (fcad495, 2026-08-16) |
| **011** | **백엔드 일관성 3건 (미대기 저장·상한 없는 RPC·사실과 다른 주석)** | P2 | S | — | DONE (5b1b5bc, 2026-08-16) |
| **012** | **billing/confirm 금전 상태 전이 계약 테스트 (백로그 1번 착수)** | **P1** | M | — | DONE (2026-08-16) |
| **013** | **auth 미들웨어 계약 테스트 (JWT 만료 우회·삭제 유예 화이트리스트)** | P2 | S | — | DONE (e46f101, 2026-08-16) |
| **014** | **report.js 점수 엔진 계약 테스트 (지역판정·상대나이 실사고 2건 고정)** | **P1** | M | — | DONE (5b94815, 2026-08-16) |
| **015** | **cron.js `authorizeCron` 계약 테스트** | P2 | S | — | DONE (bbb1a61, 2026-08-16) |
| **016** | **규제 판정 두 경로 정합 — `isRegFront` lawdCd 게이트 (실결함 수정)** | **P1** | S | — | DONE (80d341b, 2026-08-16) |
| **017** | **KOSIS 미분양 Redis 2차 캐시 역이식 (+Map 직렬화 함정 차단)** | P2 | S | — | DONE (abbf772, 2026-08-16) |
| **018** | **서울 규제 해제 시 LTV 라벨이 스냅샷을 따라가게 (016 잔여)** | P2 | S | 016 | DONE (c755b38, 2026-08-16) |
| **019** | **마이그레이션 실적용 전수 대조 + advisor 경고 3건 해소** | **P1** | M | — | DONE (514352c, 2026-08-16) |
| **020** | **정확성 전용 최소 ESLint 도입 + 프론트 인라인 JS 커버 + CI Node 24 정합** | P2 | M | — | DONE (d6511c8·6399333, 2026-08-16) |
| **021** | **gitleaks v3 SHA 재고정(v2 EOL) + 의존성 상향/engines>=22 + RLS role 복원** | **P1** | M | — | DONE (2e6e38c·f3227f8·4f3f17b, 2026-08-16) |
| **022** | **서울 중구 규제판정 불일치 수정 + 계약 테스트를 25개 구 전수로** | **P1** | S | 016 | DONE (2097305, 2026-08-16) |
| **023** | **중개보수 경계 회귀 복구 — tier 헬퍼 경계 의미별 분리 (내가 만든 회귀)** | **P1** | S | 008 | DONE (eefcb8f·0fea3f6, 2026-08-16) |
| **024** | **무인증 /api/health 의 사용자 챗 원문 노출 차단 (개인정보)** | **P1** | S | — | DONE (2f6c0be, 2026-08-16) |
| **025** | **결제 목이 .eq() 인자 기록 → CAS·소유자 필터 단언** | **P1** | S | 012 | DONE (f83a9f4, 2026-08-16) |
| **026** | **오늘 적용한 DDL 3종 마이그레이션 사후 기록** | P2 | S | 019 | DONE (95c64c8, 2026-08-16) |
| **027** | **규제 판정 네 번째 사본(report.js) 스냅샷 정합** | **P1** | S | 018 | DONE (a81b261, 2026-08-16) |
| **028** | **/popular stale 폴백 화면 표기** | P2 | XS | — | DONE (2a20ec2, 2026-08-16) |
| **029** | **`.env.example` 드리프트를 CI 가 잡게 (같은 누락 3번째)** | **P1** | S | — | DONE (2026-08-17) — 계획은 누락 1건 가정이었으나 기계 대조로 **2건** 발견 |
| **030** | **검색 두 핫패스에서 의존 없는 조회 병렬화 (in-bounds·facility)** | P2 | S | — | DONE (c8e2e27, 2026-08-17) |
| **031** | **사실과 다른 문서 3곳 정정 + 죽은 의존성 `postgres` 제거** | P2 | S | — | DONE (2026-08-28) — 계획서의 "72 pass" 는 계획 시점 값, 실측 **106 pass** 로 기재 |
| **032** | **cron 데드라인 가드 이식 + 외부 API 팬아웃 동시성 상한** | P2 | S | — | DONE (2026-08-28) |
| **033** | **환불 self-service — 설계·스파이크(코드 무변경)** | P2 | S | — | DONE (2026-08-28) — Q1~Q5 답변 채움, 구현은 034 |
| **034** | **환불 self-service 구현 — `GET /billing/payments` + 결제내역·환불 버튼** | P2 | M | 033 | DONE (2026-08-28) — 계약 테스트 1건 신규(107 pass). 결제 오픈 후 사후 검증은 계획서 별도 절 |
| **035** | **`/share` 치환 문자열의 `$` 패턴 확장 차단 (20자 쿼리 → 35.8MB 실측)** | **P1** | S | — | DONE (ca4fb8a·d2d7972, 2026-09-06) — 실측: 확장패턴 20자 35.8MB → ×1.00 · 부재 계약 후속 추가(한 겹만 되돌려도 잡힘) |
| **036** | **비조정지역 '2주택+' 취득세를 보수적 값으로 (3주택 이상 4,500만원 과소 안내)** | **P1** | S | ⚠036·038 동시 실행 금지(같은 파일) | DONE (cb23d71, 2026-09-06) — 기존 계약 테스트 2개가 09-02 버그를 정답으로 고정하고 있어 함께 갱신 · 양방향 주입 각 fail 3 |
| **037** | **전월세 열화 응답이 공유 캐시에 8일 굳는 것 차단 + 미커버 분기 테스트** | **P1** | S | — | DONE (c551ab2, 2026-09-06) — rentWarm.js 무변경 확인(계획 전제 성립) · 주입 3건 전부 검출 |
| **038** | **상세 첫 탭 히어로 필드명 — 추천 경로에서 셀 2개·표본 배지 유실** | **P1** | S | ⚠036 과 같은 파일 | DONE (113ed8f, 2026-09-06) — 리뷰어 주입 2건 검출 · 라이브 확인 미수행(워크트리에 dev 서버 없음) |
| **039** | **추천 예산 상한·컷·게이트를 소스 검사 → 실제 실행으로 승격 (프로덕션 코드 무변경)** | **P1** | M | — | DONE (96f1d8b, 2026-09-06) — 프로덕션 0 변경 · ⚠ 계획서 주입 5건은 정규식도 전부 잡았고 진짜 간극은 "같은 줄 두고 주변 로직 무력화" 형태(리뷰어 독립 재현) |
| **040** | **실패가 스스로 복구되게 — 지역 경신 백오프·관심도 워밍 기아·브리핑 비수렴 3종** | P2 | S | — | DONE (321245f, 2026-09-06) — DDL 무추가 · 커밋 3분리 · 주입 2건 검출. ⚠ 회전 도입이 기존 테스트를 날짜 의존으로 만든 것을 실행자가 실측 발견해 dayIdx 고정 |
| **041** | **절대 룰 ① 을 결정론 카드에도 (AI 채널만 정화돼 있었다)** | P2 | S | — | DONE (a21b674, 2026-09-06) — 점수 산식·프론트 무변경 · 주입(등급 라벨 복원) → fail 2(문자열 계약 + 실행 테스트가 독립 검출) |
| **042** | **express major 를 dependabot 예외로 — "보이지 않는 기본값" 을 결정으로 (마이그레이션 아님)** | P2 | S | — | DONE (문서 기록만, 2026-09-06) — ⚠ **설정 변경은 공식 정책상 불가**(아래 결정 기록 참조). 코드·설정 변경 0 |
| **043** | **계측기의 나머지 절반 — `search`·`report` 이벤트 전송** | P2 | S | — | DONE (3ea669f, 2026-09-06) — ⚠ 두 번 반려: ① 가드가 `_notice`(원소 1개 배열)를 못 걸렀다 ② 단언이 속성명을 안 봤다. 최종 주입 3건 전부 검출 |
| **044** | **(스파이크) 고아 페이지 12,000개를 링크 그래프에 — 4주 측정 계획 포함** | P3 | M | 043 권장 | TODO |
| **045** | **(스파이크) 공개 페이지 → 앱 CTA 가 맥락을 버린다 — 열린 질문 2개 먼저** | P3 | S | 043 권장 · 044 와 함께 | TODO |
| **046** | **분석 카드 전세가율의 표본 완전성 표기** | P2 | S | — | DONE (81724c2, 2026-09-06) |
| **047** | **KST 하루 경계 SSOT 단일화 (사본 3벌 + monthsWindow 로컬 TZ)** | P2 | S | — | DONE (87fde52, 2026-09-06) |
| **048** | **molit 매퍼 3벌 중 세 번째 `jibun` 누락** | P2 | S | — | DONE (edf0977, 2026-09-06) |
| **049** | **사실과 다른 문서 정정 + `npm run verify` 게이트 통합** | P2 | S | — | DONE (452d543, 2026-09-06) |
| **050** | **backend test 를 TZ=UTC 로 강제 (로컬 초록이 프로덕션을 대변 못 함)** | P2 | XS | 047 | DONE (ed8d658, 2026-09-06) — 계획서 파일 없음(실행 중 발견해 즉시 처리) |
| **051** | **챗 단지 검색이 정식명·지역+단지명을 못 찾는다 (정식명의 76.6% 미도달)** | **P0** | M | — | TODO |
| **052** | **검색창 자동완성도 공백 든 질의를 못 찾는다** | P1 | S | — | TODO |
| **053** | **`molit_aliases` 실제 backfill (14,661행 중 1행) + 거짓 문서 정정** | P1 | M | ⚠ **운영자 DB 승인 필요** | TODO |

### 051~053 라운드 (2026-09-06, 운영자 실사용 실패 신고에서 출발)

운영자가 도우미 채팅에 **"공릉 풍림아이원 시세"** 를 입력해 *"국토부 실거래 데이터에서 찾지 못했어요"*
를 받았다. 프로덕션 DB 읽기 전용 조회로 원인을 확정했다.

| 실측 | 값 |
|---|---|
| `apt_master` 단지 수 | 14,661 |
| 챗이 도달 가능한 단지 | **3,427 (23.4%)** |
| 챗이 못 찾는 단지 | **11,234 (76.6%)** |
| 서울 3,397 중 화면에 "찾지 못했어요"가 뜨는 단지 | **2,482 (73.1%)** |
| 못 찾는 것 중 같은 동에 유사 MOLIT 이름이 실재 | **4,888** |
| `molit_apt_index` 이름 중 공백 포함 | 189 / 22,473 (**0.8%**) |
| `apt_master` 이름 중 공백 포함 | 2,251 / 14,661 (**15.4%**) |
| `apt_master.molit_aliases` 가 비어 있지 않은 행 | **14,661 중 1** |

원인 3가지: ① 챗이 `molit_apt_index` 만 보고 `apt_master`·alias 를 안 본다
② 질의 공백을 정규화하지 않아 "지역 단지명" 형태가 원리적으로 0건
③ 못 찾으면 대안 없이 포기한다.

⚠ **유사도 단독 매칭 금지**(IDENTITY-GATE). 이번 조사에서도 `강일리버파크11단지` ↔ `강일리버파크1단지`
(trigram 0.750) 오매칭이 재현됐다. 자동 채택은 정규화 완전일치 · alias · A/B 형제 규칙만.

**적재 범위는 결함이 아니다** — `molit_transactions` 가 9개 시도(125 시군구)만 있는 것은
`LAWD_CODES` 정의대로이고, 광주 제외는 운영자 명시 지시다
(`transactionService.js` `REGION-SWAP-2026-08-10`: *"광주말고 청주로. 전라도 쪽은 하지마"*).

### 035~045 라운드 (2026-09-06, `/improve` 감사)

**감사 범위를 좁힌 근거.** 2026-09-02 전면 재감사 + 09-05 재대조가 저장소 전체를 훑었고
로드맵 P0~P3 가 대부분 닫혀 있다. 전체를 다시 훑으면 아는 답을 재생산할 뿐이므로,
**아무도 본 적 없는 구간**만 봤다: `285ef31..HEAD` — **51커밋 · 40개 파일 · +5,035줄**
(추천 엔진 전면 재작업 · rentService 페이서·Redis 캐시 · OG 이미지 신규 · warm-rent cron ·
price_records 성능 마이그레이션 · SSR 공유 페이지).

서브에이전트 4개 병렬(정확성 / 보안·의존성 / 성능·테스트 / 부채·DX·문서·방향성) →
**모든 인용을 상위 세션이 직접 열어 재검증**. 서브에이전트 주장 3건을 기각했다(아래 참조).

**감사 시점 기준선(실측)**: 테스트 **223 pass · 0 fail** · `npm run lint` 초록 ·
`check-deps-sync` 외부 16개 · `check-env-example` 참조 62/선언 59/플랫폼 10 ·
`security-regression-check` 15패턴 위반 0. 기준 커밋 `e7dc1c6`.

**운영자 선택(2026-09-06)**: P1 6건 + P2 워밍/백오프 3종 + 방향성 3건 전부 + 판정 2건 전부.

- **035 가 P1 인 이유**: 추정이 아니라 **재현해서 측정했다**. `apt` 에 확장 패턴 10쌍(20자) →
  응답 35,784,867자(원본 823,777자의 **43배**), 20쌍(40자) → `RangeError: Invalid string length`.
  `apt` 상한이 60자이므로 **상한 입력은 항상 RangeError 구간**이다. 무인증·무리밋 공개 경로이고
  결제·인증·cron 이 같은 서버리스 함수에 있다.
- **036 은 09-02 에 내가(=이전 라운드가) 만든 회귀다.** `ACQ-REG-CALC-2026-09-02` 가
  `'2주택+'` 칩(2주택과 3주택 이상을 구분하지 않는다)에 비조정 기본세율을 적용했다.
  같은 함수 바로 위 주석이 "모를 때 낮게 안내하면 과소 안내가 된다" 고 스스로 적어 뒀는데
  그 원칙을 어긴 것이다. **칩 분할이 근본 해결이나 문자열 비교 지점이 프론트 12·백엔드 5곳이라
  L 급 + UI 라 디자인 기획 선행** → 이번엔 보수적 복원만 한다.
- **037 이 #11(5분 오류 캐시)을 포함하는 이유**: 비정상 resultCode 를 `throw` 로 올리면
  기존 catch 가 `cache.set(cacheKey, [], 300)` 을 심고, 그 5분 안의 다음 호출이 예외 없이 `[]` 를
  돌려줘 `monthsFailed` 가 다시 빈다. **#11 을 함께 고치지 않으면 037 의 목적이 달성되지 않는다.**
  운영자가 개별 선택한 항목은 아니지만 같은 결함의 나머지 절반이라 포함했고, 계획서에 그 사실을 적었다.
- **039 는 프로덕션 코드를 한 줄도 바꾸지 않는다.** 이 저장소가 2026-09-02 에 4건 해낸
  "텍스트 → 행위" 승격을 추천 엔진에 적용한다. Step 4 의 **회귀 주입 5건 결과표가 실제 산출물**이다.
- **042 는 마이그레이션이 아니다.** express 4 를 유지하되, `ignore: "*" major` 때문에
  express 5 PR 이 영구히 생성되지 않아 "지금은 안 올린다" 가 **결정이 아니라 보이지 않는
  기본값**이 된 상태를 고친다. `gitleaks-action@v2` 선례와 같은 계열이다.
- **044·045 는 설계·스파이크다**(playbook 의 direction 지침). 044 는 최소 변경 1건 + **4주 측정
  계획**이고, 045 는 **열린 질문 2개에 먼저 답하는 것**이 본체다. 045 의 결론이 "구현하지 않는다"
  여도 정상적인 완료다.
- **실행 순서 권고**: 035 → 036 → 038 → 037 → 039 → 040 → 041 → 042 → **043** → 044 → 045.
  043 을 044·045 보다 먼저 넣어야 그 효과를 볼 수 있다.

### 029~033 라운드 (2026-08-17, `/improve` 감사)

오늘(08-16) 이미 correctness·security·tests 를 52건 감사·재검증했으므로, 이 라운드는 **미감사 영역만**
봤다: performance · direction · DX/docs/deps · 그리고 **08-16 에 만든 코드 67커밋 자체**.
서브에이전트 4개 병렬 → **모든 인용을 상위 세션이 직접 열어 재검증**한 뒤 채택했다.

- **08-16 신규 코드 67커밋 감사 결과: findings 0건.** (테스트 1472줄의 정규식 추출 패턴은 전부
  `assert.ok` 로 매치 실패를 죽이고 있고, 신규 마이그레이션 3종은 전부 권한 **축소** 방향,
  `eslint.config.mjs` 는 correctness 13규칙이 실제로 동작함을 재실행으로 확인)
- 029 가 P1 인 이유: `.env.example` 누락이 **세 번째 재발**이고, graceful-degrade 설계라
  부팅이 안 깨져서 **조용히** 반복된다. 계획 005 가 후속을 적어뒀지만 만들어지지 않았다.
- 033 은 **설계 플랜**이다(playbook 의 direction 항목 지침). 구현은 034 로 분리한다.

### 018~019 실행 결과 (2026-08-16, 운영자 승인 후 실행)

> **프로덕션 DDL 승인 근거** (감사 #20 — "승인 기록이 저장소에 없다"):
> 019(미적용 마이그레이션 적용)·021(RLS role 복원)·026(MV·RPC·인덱스 사후 기록)은 모두
> 운영자가 세션에서 **"권고대로 진행해. 승인할게. 총괄책임자의 판단으로 진행하고"** 라고 준
> 포괄 승인에 근거해 실행했다. 008~011 처럼 항목별 개별 승인은 아니었고, 그 사실을 그대로 적는다.
> 앞으로 프로덕션 DDL 은 **적용 전에 SQL 을 여기 기록하고 그 커밋을 근거로 남긴다**(절대 룰 ③).

- **018**: `_regLtvLabel` 의 서울 하드코딩을 스냅샷 조건부로. **오늘 동작 변화 0**
  (`seoulRegulated=true`), 해제되는 날에만 발동. **미로드 시엔 보수적 40% 유지** — 부팅 직후
  70% 로 찍으면 한도를 부풀리는 반대 방향 오표기가 되기 때문.
  기존 테스트가 즉시 깨졌다(하네스가 새 `window` 의존성을 안 넘겨줌) → 하네스 갱신.
- **019**: ⚠ **미감사로 남아 있던 "마이그레이션 실적용 여부"를 종결.** 27개 파일 선언 vs
  `pg_catalog` 전수 대조.
  · ✅ 함수 3종 하드닝·pg_trgm 스키마·dedup_key v2·인덱스 5종·pg_cron job 전부 **적용 확인**
  · ✅ **오탐 정정** — `billing_plans` 는 정책 0개가 아니라 `billing_plans_public_read` 존재
  · ❌ **`20260504000002`(regulations overlap GIST) 미적용 발견** → overlap 0건 선검증 후 적용,
    의도적 위반 INSERT 가 `exclusion_violation` 으로 막히는 것까지 실증(롤백, 데이터 변화 0)
  · advisor security 경고 **6 → 3**: btree_gist public→extensions, SECURITY DEFINER RPC 2종의
    anon/authenticated EXECUTE 회수(호출부가 service_role 전용임을 실측 후)

### 014~017 실행 결과 (2026-08-16)

- **014·015**: 프로덕션 코드 변경 **0**. 정규식 추출 패턴으로 8개 테스트(53→61).
  회귀 주입 **8건 전부** fail 확인.
- **016**: ⚠ **교차검증에서 나온 실결함**. `_regLtvLabel`(lawdCd 우선)과 `isRegFront`(문자열 전용)가
  독립 구현이라 **부산 강서구(26440)** 에서 갈렸다(`LAWD_CODES` 전수 대조 — 어긋나는 곳은 이 1곳뿐).
  같은 모달에서 "LTV 70% 비규제" ↔ "취득세 2주택 8% 조정 중과" ↔ "6개월 전입 의무"가 동시 표시.
  → `isRegFront(regionStr, lawdCd)` 로 동일 게이트 적용 + 호출부 4곳 전파.
  **두 함수가 서로 같은지를 단언하는 계약 테스트** 추가(한쪽만 고치면 걸린다).
- **017**: Map 은 JSON 왕복을 못 견딘다(`all.map.get is not a function`, Node 실측) →
  pack/unpack + fail-safe. 함정 자체를 테스트로 고정.

⚠ **주입 실험 프로토콜 정정**: `git checkout` 원복은 파일 단위라 **미커밋 수정까지 날린다**
(오늘 실제로 Plan 016 수정이 통째로 사라졌고 `grep -c` 로 발견해 재적용했다).
→ **회귀 주입은 실제 수정을 커밋한 뒤에만** 실행하고, 주입 전 `git status --short` 가 비었는지 확인할 것.

### 008~011 실행 결과 (2026-08-16, 운영자 승인 후 실행)

- **008**: 프론트 `_pickTierRate` `<` → `<=`. 계약 테스트 추가(41→42). **회귀 주입 검증**:
  연산자를 되돌리면 fail 1 → 원복 후 42 pass.
- **009**: `CREATE INDEX CONCURRENTLY idx_molit_deal_date (deal_date DESC)`.
  **2,236ms → 0.119ms**(`Sort` 노드 소멸, `Heap Fetches: 0`, `indisvalid: true`). 코드 변경 0.
- **010**: `_escHtml(d.apt||'')` 적용 + 중복 `signal` 키 제거(90s 유지). 가드 13→14패턴.
  **가드 회귀 주입 검증**: escape 되돌리면 `frontend/index.html:8267` 지목하며 exit 1.
- **011**: `await storePopularSnapshot`, `MV_REFRESH_ABORT_MS=60000`(실측 11,451ms 근거),
  degraded 주석 정정.

⚠ **실행 방식 메모(2026-08-16 시점)**: `execute` 의 worktree 격리를 쓰지 못해 계획서의 단계·검증을
그대로 따라 직접 실행했다.

> ✅ **2026-09-06 정정 — 위 서술의 원인 진단이 틀렸다.** "상위 디렉터리가 git repo 가 아니라
> worktree 생성이 실패한다" 고 적었으나 `git worktree add` 는 **정상 작동한다**(실측).
> 실패했던 것은 Agent 도구의 `isolation:"worktree"` 옵션이지 git 자체가 아니었다.
> 2026-09-06 라운드는 워크트리 5개에 실행자 5명을 붙여 **완전 격리 병렬 실행**했고 커밋 섞임 0.
> 절차·함정(짧은 경로 · node_modules junction 공유 · EOF 충돌 해소 · **정리 시 junction 먼저 끊기**)은
> 메모리 `concurrent-sessions-shared-worktree` 에 기록했다.

⚠ **계획서 criteria 결함 1건(011)**: `grep -c "fire-and-forget" backend/routes/search.js → 0`
을 파일 전체 대상으로 썼는데, 9행의 **다른 라우트**(`/search/history`) 헤더 설명에도 같은 표현이
있어 문자 그대로는 만족 불가였다. 범위 밖 코드를 건드리지 않았다.
(참고: 그 헤더 설명은 **부정확**하다 — `/search/history` 는 실제로 `await` insert 후 결과를
응답한다. 별건이라 이번에 고치지 않았다.)

Status values: TODO | IN PROGRESS | DONE | BLOCKED (한 줄 사유) | REJECTED (한 줄 근거)

## Dependency notes

- **008~011 은 상호 의존이 없다 — 병렬 실행 가능.** 단 008 과 010 은 둘 다 `frontend/index.html`
  을 수정하므로 **동시에 돌리지 마라**(같은 파일 충돌). 순차로 하되 순서는 무관하다.
- 009 는 코드 변경이 0 이고 DB 만 바꾸므로 다른 계획과 완전히 독립이다. 단 **운영자 승인 없이는
  Step 2 를 실행할 수 없다**(계획 안의 "실행 게이트" 참조).
- 검증 공통 게이트: `cd backend && npm test` + `node scripts/security-regression-check.js`.
  ⚠ **아래 숫자는 계획 008~011 당시의 값이다(41 pass·13패턴). 지금 baseline 이 아니다** —
  2026-08-16 감사 후속으로 **71 pass · 15패턴**까지 올라갔다(계획 012~028 + 감사 확정 4건).
  당시 기준: 008 은 42 pass, 010 은 14패턴이 됐다. 새 계획을 쓸 때는 baseline 을 **그 시점에 실측**할 것
  — 여기 적힌 숫자를 그대로 베끼면 낡은 값을 재생산한다.

## 2026-08-16 감사에서 발견했으나 이번에 계획화하지 않음 (백로그)

운영자가 이번 라운드에서 008~011 만 선택했다. 아래는 **유효한 발견이지만 미착수** —
재감사할 필요 없이 여기서 바로 계획으로 승격할 수 있다.

- ~~**`billing.js` 결제 로직 테스트 0**~~ → **012 로 부분 착수 완료(2026-08-16)**.
  confirm 경로 3건 고정: 키 미설정 시 501 차단 · 금액 불일치 → 400 + `failed` 전이(동일 orderId
  재사용 차단) + 실패 기록에 금액 미포함(PIPA) · 이미 captured 면 Toss 재호출 없이 멱등.
  **프로덕션 코드 변경 0** — express 라우터 스택에서 핸들러만 꺼내 req/res 목으로 호출했다
  (결제 로직을 테스트 편의로 리팩터링하는 것이 더 위험하다는 판단).
  **회귀 주입 2건 검증**: 금액 비교 무력화 → fail 1, captured 멱등 분기 제거 → fail 1.
  **→ 012-2(2026-08-16)로 webhook·환불까지 완료.** 총 **9건**(42→51 테스트 — 착수 직전 baseline 은 41 이 아니라 42 였다):
  · webhook: Toss 재조회 orderId 불일치 → 400(위조 차단) · 정적 시크릿 불일치 → 401 ·
    금액 불일치라도 **terminal(captured)은 failed 로 덮지 않고 200**(Toss 재시도 중단)
  · 환불: **7일 창 경계 양쪽**(8일 → 400 + Toss 미호출 / 6일 → Toss 호출 도달) ·
    captured 아니면 409 · 이미 refunded 면 멱등 200
  · axios 는 `require.cache` 스텁, 7일 경계는 `approved_at` 조작으로 **타이머 제어 없이** 검증.
  **회귀 주입 4건 전부 확인**(금액비교·멱등분기·7일창·orderId검증 각각 무력화 시 fail 1).
  ⚠ **이 과정에서 실제 결함 1건 발견·수정**: confirm 은 P2-5(2026-05-04)로 실패 기록에서 정확한
  결제 금액을 뺐는데 **webhook 만 그대로**였다(로그·failure_reason 양쪽). 같은 방어선인데 서로 다른
  개인정보 정책을 갖고 있던 셈 — 오늘 취득세와 같은 "한쪽만 고침" 패턴이라 confirm 과 맞추고
  계약 테스트로 묶었다.
- ~~**`middleware/auth.js` 테스트 0**~~ → **013 으로 완료(e46f101)**. 다만 `_jwtExpMs` 의
  base64url 복원 두 줄은 Node 에서 no-op 이라 **어떤 테스트로도 고정 불가**(주입해도 안 잡힘).
- ~~**`report.js` 점수 엔진 export·테스트 0**~~ → **014 로 완료(5b94815)**.
  export 조차 추가하지 않았다 — 정규식 추출로 **프로덕션 코드 변경 0**.
- ~~**KOSIS 미분양 로더 Redis 2차 캐시 누락**~~ → **017 로 완료(abbf772)**.
  단순 역이식이 아니었다: 반환값의 `map` 이 Map 인스턴스라 그대로 실으면 복원 시 TypeError.
- ~~**`cron.js` `authorizeCron` 테스트 0**~~ → **015 로 완료(bbb1a61)**.
- ~~**린트 도입**~~ → **020 으로 완료(d6511c8, 2026-08-16).** ⚠ **아래 미착수 판단은 틀렸다.**
  스크래치패드에 eslint 를 설치해 실측하니 **backend 에러 0 · 프론트 인라인 에러 1**(그것도 의도된
  게이트)이었다 — "위반이 대량으로 나올 것"이라는 전제가 사실이 아니어서 baseline 전략 자체가
  불필요했다. **측정 가능한 것을 추정으로 판단해 '안 한다'를 정당화한 사례**로 남긴다.
  (원문 보존 ↓)
- ~~**린트 도입** (S~M) — 2026-08-16 판단: 지금은 하지 않는다. 운영자 결정 대기.~~
  전제(=`no-dupe-keys` 같은 표준 규칙이면 계획 010 의 결함 B 를 잡았다)는 **사실**이다.
  그러나 지금 착수하면 다음이 전부 딸려온다:
  · `eslint`/`acorn`/`espree`/`typescript` 중 **로컬에 설치된 파서가 하나도 없다**(실측).
    즉 네트워크 설치 없이는 시범 측정조차 불가능하다.
  · devDependency 추가는 `scripts/check-deps-sync.js` 게이트 때문에 **루트+backend 양쪽
    package.json 동기 수정**이 필요하다(이 게이트는 실제로 CI 를 막은 이력이 있다).
  · 11,070행 단일 파일 프론트에서 기존 위반이 대량으로 나올 것이라 **baseline 전략**
    (신규/변경 파일만 검사 or 규칙 최소 집합부터)이 선행돼야 한다.
  ⚠ **파서 없이 정규식으로 흉내 내는 가드는 만들지 말 것** — 중복 키는 문법적으로 합법이라
    `node -c`·`new Function()` 로는 절대 안 잡히고, 정규식 스캐너는 오탐/누락을 검증할 방법이
    없어 "가드가 지켜준다"는 잘못된 확신만 만든다.
  권고: 규칙을 `no-dupe-keys`·`no-unreachable`·`no-dupe-args` 정도로 좁힌 최소 설정 +
  **변경된 파일만** 검사하는 CI 스텝으로 시작.
- ~~**`gitleaks-action@v2` 가변 태그 → 커밋 SHA 고정**~~ → **완료(b94d995, 2026-08-16)**.
  `ff98106`(= v2 annotated tag 가 가리키는 commit)로 고정. dependabot 이 SHA 핀도 갱신한다.
  `actions/checkout`·`setup-node` 는 1st-party 라 미고정 — 필요 시 같은 방식으로.

### 2026-08-16 라운드에서 새로 올라온 항목 (운영자 판단 필요)

- ~~⚠ **`_regLtvLabel` 의 서울 하드코딩**~~ → **018 로 완료(c755b38)**. 다만 **부분 해제**
  (예: 강남3구만 규제 유지)는 스냅샷의 `seoul` 이 문자열 + `!!` 로 boolean 화되는 구조라
  **표현 자체가 불가능**하다. 그 경우 두 판정 함수는 서로 일치하되 **둘 다 부정확**하다.
  프론트에서 못 고친다 — `regulationsService` 의 `regulatedRegions` 스키마부터 손봐야 한다.
- ⚠ **Supabase `auth_leaked_password_protection` 비활성** (XS, **운영자 Dashboard 조치**):
  advisor WARN. HaveIBeenPwned 대조로 유출된 비밀번호 사용을 막는 기능인데 꺼져 있다.
  SQL 이 아니라 Dashboard → Authentication 설정이라 코드/MCP 로 켤 수 없다.
- ~~**RLS 정책 role 드리프트**~~ → **021 로 완료(4f3f17b)**. `ALTER POLICY … TO authenticated` 5건.
  안전성 근거: `fieldNotes.js` 가 사용자 JWT 클라이언트(`getUserScopedClient`)를 쓰므로 role 이
  `authenticated` 로 해석되고, 나머지 경로는 service_role(RLS 우회), 프론트 직접 접근은 0건(실측).
  (원문 ↓)
- ~~**RLS 정책 role 드리프트** (S, 심층방어): `field_notes_*` 4개 + `ai_feedback_select_own` 의~~
  role 이 `authenticated` 가 아니라 **PUBLIC** 이다(20260504000003 이 DROP+CREATE 하며 `TO` 절
  누락, 20260531000001 이 `insert_own` 만 복원). **기능 영향은 0** — USING/WITH CHECK 가
  `(SELECT auth.uid()) = user_id` 라 anon 은 `auth.uid()` 가 NULL 이어서 통과 못 한다(실측).
  뚫린 구멍이 아니라 **의도와 실제의 불일치**라 019 에서 건드리지 않았다.
### dependabot PR 처리 결과 (2026-08-16) — ⚠ **#58 은 머지하지 말 것**

- **#65** minor-and-patch 4종 → **직접 적용 완료(f3227f8)**. PR 을 머지하지 않은 이유: PR 생성 이후
  루트 package.json 이 갈라졌고(eslint devDep·scripts), 무엇보다 **`engines` 상향이 PR 에 빠져 있어**
  그대로 머지하면 supabase-js 요구(>=22)와 선언(">=20")이 어긋난 채 남는다. dependabot 이 곧 자동 종료한다.
- ~~⚠ **#58** `gitleaks-action 2→3`~~ → **닫음(2026-08-16)**. `gh pr diff 58` 로 실제 내용을 확인한 결과
  `uses` 를 **가변 태그 `@v3`** 으로 바꾸는 것이 맞았다 — 머지하면 `@e0c47f4… # v3.0.0` SHA 고정이 풀린다.
  v3 전환 자체는 `2e6e38c` 로 이미 달성했으므로 사유를 PR 코멘트로 남기고 닫았다.
- **#62** `setup-node 6→7` · **#61** `checkout 6→7` — GitHub 1st-party 액션 메이저. 머지는 운영자 결정.

#### v2 를 SHA 로 고정한 것이 왜 위험했나 (재발 방지)
`gitleaks-action@v2` 는 node20 런타임이고 업스트림이 명시한 일정이 있다:
**2026-06-02** 러너 기본이 Node 24 로 전환(우회 env 필요) → **2026-09-16 Node 20 제거로 v2 완전 중단**.
즉 v2 에 SHA 를 고정하는 것은 한 달 뒤 Secret Scan job 을 죽이는 선택이었다.
→ **버전 고정 대상은 "지금 도는 것"이 아니라 "앞으로도 도는 것"이다. 고정 전에 그 버전의 수명을 확인할 것.**

<!-- 아래는 처리 전 시점의 기록 -->
- **dependabot PR 4건 대기 — 머지는 운영자 결정** (2026-08-16 실측):
  · **#65** minor-and-patch 4종(`supabase-js` 2.105→2.112 · `anthropic-sdk` · `upstash/redis` · `axios`).
    ⚠ 신규 `supabase-js` 가 `engines.node ">=22"` 를 요구한다. CI 는 이번에 24 로 올렸으니 정합이지만
    **`package.json` 의 `engines` 는 아직 `">=20"`** 이라 함께 올리는 것이 맞다.
  · **#62** `setup-node 6→7` · **#61** `checkout 6→7` — GitHub 1st-party 액션 메이저.
  · **#58** `gitleaks-action 2→3` — ⚠ 오늘 v2 를 **커밋 SHA 로 고정**했으므로 이 PR 과 충돌한다.
    3 으로 갈 거면 **v3 의 SHA 로 다시 고정**할 것(태그로 되돌리면 오늘 한 공급망 조치가 무효화된다).
- ⚠ **카카오 로그인 게이트 — 전제조건은 이미 충족됨** (S, **운영자 확인 필요**):
  `frontend/index.html` 의 `loginKakao()` 는 `return;` 으로 막혀 있고 주석은 "사업자등록 후 활성화"
  라고 적혀 있다. **사업자등록은 2026-08-09 완료**됐다. 그런데도 해제하지 않은 이유는 KOE205 가
  카카오 개발자 콘솔의 앱/비즈앱 설정 문제라 사업자등록만으로 풀리지 않고, 콘솔 상태를 코드에서
  확인할 방법이 없기 때문이다. 콘솔 확인 후 해제 여부를 결정할 것.
- ~~**`kosisService.KOSIS_CACHE_KEY` 는 죽은 export**~~ → **제거 완료(680df79)**. 소비처 0건 실측 후
  제거. 캐시가 node-cache + Redis 2계층이라 키 하나 노출은 "밖에서 무효화 가능"이라는 잘못된
  기대만 만든다 — 필요해지면 두 계층을 함께 지우는 함수를 노출할 것(근거를 주석에 남김).
- ~~**`/search/history` 헤더 주석이 부정확**~~ → **정정 완료(680df79)**. 실제로는 insert 를 await 하고
  `.select('id, created_at').single()` 결과를 **201 로 응답**한다(실패는 `next(e)`).
  주석을 믿고 "응답 안 기다려도 된다"고 판단하면 기록 유실을 오판하는 지점이었다.

## 2026-09-06 감사에서 확정했으나 이번에 계획화하지 않음 (백로그)

운영자가 P1 6건 + P2 워밍/백오프 3종만 선택했다. 아래는 **전부 상위 세션이 인용을 직접 열어
확인한 유효한 발견**이며, 재감사 없이 여기서 바로 계획으로 승격할 수 있다.

- **보고서 캐시 히트 환불이 그 목적 케이스에서 도달 불가** (S, HIGH).
  `middleware/dailyLimit.js:185-187` 이 카운터를 **먼저 올리고** `used > effectiveLimit` 이면
  429 로 조기 반환한다. 로그인 free 는 `limit 0 + loggedInBonus 1` = **1**(`server.js:299`)이라
  2회차는 핸들러에 도달하지 못한다 → `routes/report.js:158-164` 의 환불 코드가 실행되지 않는다.
  주석이 근거로 든 "타임아웃 뒤 재시도" 가 정확히 그 케이스다. 도달 가능한 유일한 경우는
  **그날 첫 요청이 남의 캐시에 맞은** 교차 사용자 히트뿐. 프론트(`index.html:4685`)는
  "같은 조건으로 다시 생성하면 바로 받을 수 있어요" 라고 안내한다 — **안내와 동작이 어긋난다.**
  ⚠ 한도 우회 표면이라 수정 시 캐시 키 위조로 무한 생성이 되지 않는지 확인이 필요하다.
- **`supabase/schema.sql` 스냅샷이 2026-09-05 DDL 미반영** (S, HIGH).
  `20260905_price_records_perf.sql:15` 이 만든 `idx_molit_aptseq_area_date_amt` 가 스냅샷에
  **0건**이고, `:24` 가 드롭한 `idx_molit_aptseq_area_date` 는 `schema.sql:434` 에 남아 있다.
  `get_price_records`/`_by_region` 본문도 perf 이전 형태다. ⚠ 이 저장소가
  **"확정 근거"로 지정한 파일**이 하필 최근 바뀐 객체에서 프로덕션과 어긋났다.
  게다가 `schema.sql:25` 이 말하는 "생성 스크립트" 는 `scripts/` 에 **존재하지 않는다**.
  → pg_catalog 재조회로 교체 + "마이그레이션의 CREATE/DROP 대상이 스냅샷과 정합한가" 계약 테스트.
- **`jibun` 매퍼 3벌 중 세 번째만 누락** (S, HIGH, latent).
  `transactionService.js:67/94`(있음) · `:132/169`(2026-09-05 에 추가) · `:792/803-810`(**없음**).
  세 번째는 `getTransactionsByAptSeq` 이고 `/apt/:seq` SSR·`/api/og/apt/:seq` 가 탄다.
  지금은 화면에 안 드러나지만, 직전 스프린트가 정확히 이 누락 하나로 실사고를 냈다.
- **KST 하루 경계 SSOT 를 만들고 3벌을 안 옮겼다** (S, HIGH).
  `utils/kstTime.js:10` 이 "'하루' 경계 계산을 한 곳에" 라고 적었는데 임포터는
  `geocodeCacheService.js:111` 과 `utils/txWindow.js:24` 둘뿐이다. 자체 사본:
  `middleware/dailyLimit.js:41`(무료 한도 리셋 — **실사고 경로**) · `routes/account.js:348`(PIPA 유예 연도) ·
  `services/briefingService.js:27`(브리핑 날짜 키). 여기에 더해
  `services/rentService.js:52-57` 의 `monthsWindow` 가 **호스트 로컬 TZ 메서드**를 쓴다
  (프로덕션 UTC → 매월 1일 KST 00~09시에 6개월 창이 한 달 밀린다).
  ⚠ SSOT 파일이 존재한다는 사실이 오히려 "이미 해결됨" 이라는 거짓 안심을 준다.
- **문서 드리프트 묶음** (S, HIGH). 전부 실측 대조:
  · `README.md:169`·`:177`·`:180` 이 **삭제된 엔드포인트 3개**를 광고(`/api/properties/info` ·
    `/api/regulations/ltv` · `/api/analysis/total-cost`) · `:148` 의 mount 줄번호가 틀림(실제 249–325)
  · `README.md:129`·`:210` 이 cron **10개**라고 적었으나 `vercel.json` 실측 **14개**
    (누락: `warm-interest` · `warm-rent` — 둘 다 직전 51커밋에서 추가)
  · README 에 **공개 SSR 표면 언급이 0건**(`/apt` · `/region` · `/briefing` · `/sitemap.xml` · `/api/og`)
    — 최근 세 스프린트의 산출물이자 지금 제품의 정면인데 문서는 여전히 "SPA + API" 를 설명한다
  · README 배포 env 목록 12개 vs `.env.example` 59개 (빠진 것이 **결제·Sentry·웹푸시·카카오**)
  · `CLAUDE.md:97`·`:99`·`:138` 이 **삭제된 모듈 2개**(`schoolClusterService` · `legalCorpusService`)와
    `/api/legal` 을 살아 있는 것처럼 안내 — 저장소에는 **부활 방지 테스트 3개**가 있어 문서와 테스트가
    정반대를 말한다 · `CLAUDE.md:78` 의 "112 pass" 는 실측 **223**(112→190→223 으로 두 번 낡음)
  · `scripts/check-env-example.js:19-21` 주석이 "CI 에서 `|| true` 로 **비차단**" 이라고 적었으나
    `ci.yml:95-100` 은 **차단 게이트**다 — 스크립트가 자기 동작을 반대로 설명한다
  → 권고: 테스트 개수처럼 **반드시 다시 낡는 숫자는 문서에서 빼고** 명령을 가리키게 한다.
- **`CLAUDE.md` 검증 체크리스트가 실제 차단 게이트 5개 중 1개만 담고 있다** (S, HIGH).
  누락: `check-env-example.js` · `check-deps-sync.js` · `security-regression-check.js` ·
  `npm run lint` · clause XSS raw-pattern. 루트 `package.json` 에 `test` 스크립트가 **없고**
  (`npm test` 는 루트에서 실패), `.husky` 없음, 전체 게이트를 한 번에 도는 명령이 없다.
  ⚠ `ci.yml:203-212` 주석은 **린트가 매달린 참조를 잡는 유일한 게이트**라고 명시했는데
  그것이 체크리스트에서 빠져 있다. → `npm run verify` 집계 스크립트 1개 + 체크리스트를
  그것만 가리키게(게이트 추가 시 두 곳을 고치지 않도록).
- **`/api/og` PNG 렌더가 fail-open 리미터 뒤에 있다** (S, MED).
  `ogImageService.js:156-157` 이 `satori()` + `new Resvg(...).render()` 를 **요청 스레드에서 동기**
  실행한다(파일 주석 실측 ~200ms). 닿는 리미터는 `generalLimiter`(60/분) 하나인데
  `middleware/rateLimit.js:15` 가 fail-open 근거를 "**비용이 제로**" 라고 적어 뒀다 —
  이 라우트가 생기면서 그 전제가 더 이상 참이 아니다. `COST_SENSITIVE_SCOPES`(`:50`)에도 없다.
  유효 `apt_seq` 가 약 15,954개라 경로만 돌리면 엣지 캐시를 계속 빗나가고,
  `robots.txt:8` 이 `Allow: /api/og/` 라 크롤러도 전량 순회한다.
  ⚠ 너무 조이면 카카오톡·X 미리보기가 카드를 못 받는다 → 전용 스코프 + 프로세스 내 동시 렌더 상한.
- **추천 팬아웃이 15곳 컷 *전* 후보 전체로 확대됐는데 주석 2곳이 낡았다** (M, HIGH 구조).
  `propertyService.js:1110` 의 `Promise.allSettled(recommendations.map(...))` 에 동시성 제한이 없고,
  `:1290-1296` 의 TRANSIT-STAGE 가 `enrichedRecs` 전체에 `nearestSubway` 를 돌린다.
  15곳 컷은 `:1329-1330` 으로 **그 뒤**다. 그런데 주석 `:1103`("top-15 로 bounded")과
  `:1118`("미매칭 항목(최대 15)에만")은 MULTI-LENS 합집합 도입 전 기준이다.
  ⚠ **성능 영향은 엔드포인트 실측이 선행해야 한다**(이 저장소는 `EXPLAIN` 시간을 매 요청 비용으로
  오해해 없는 병목을 고칠 뻔한 이력이 있다). 최소한 **낡은 주석 2줄은 정정**할 것.
- **`eok()` 금액 포매터 4벌 — 정밀도가 두 갈래** (S, HIGH).
  `aptPage.js:35`·`regionPage.js:32`·`contentDraftService.js:34` 는 `toFixed(2)`,
  `ogImage.js:24` 만 **`toFixed(1)`**. 같은 단지 중앙값이 페이지 `1.80억` / 링크 카드 `1.8억`.
  커밋 `c0ff638` 이 "문장은 페이지와 같은 함수에서" 로 문장은 통일했는데 **숫자 포매터는 사본**이다.
  ⚠ 폴백 의미가 다르다(`''` vs `null`) — 통합 시 보존할 것.
- **SSR 라우트 HTML 이스케이퍼 4벌, 그중 1벌은 의미가 다르다** (S, HIGH 사실 / 현재 악용 LOW).
  `aptPage.js:28`·`briefing.js:17`·`regionPage.js:25` 는 바이트 단위로 동일한 `esc()`,
  `share.js:30` 만 `String(s || '')` 라 **숫자 0·`false` 가 빈 문자열로 사라진다**.
  또 `og:image`/`twitter:image` 가 `aptPage` 에서는 `esc()` 를 거치는데
  `regionPage.js:54`·`:60` 과 `briefing.js:45`·`:51` 은 **날것**이다(오늘은 값이
  `/^\d{5}$/`·`/^\d{4}-\d{2}-\d{2}$/` 로 좁혀져 주입 불가 — 그래서 "fix" 가 아니라 "정합" 과제다).
  ⚠ CI 의 XSS 정적 게이트 2개(`ci.yml:165`·`:181`)는 **`frontend/index.html` 만** 본다.
- **6,663줄 단일 테스트 파일** (M, MED). `node --test` 는 **파일 단위로만** 병렬화하므로
  전체 스위트가 단일 프로세스·직렬이다. 더 중요한 것은 `require.cache` 스텁 복원(`finally`)을
  하나라도 빠뜨리면 **그 뒤 수천 줄이 오염된 스텁으로 통과**한다는 점이다 — 실패가 아니라
  **위양성 초록**이라 아무도 모른다. 223 테스트 중 **170개(76%)가 `readFileSync` 로 소스를 읽는다**.
  → 도메인별 5~7파일 분할 + `test/helpers/stub.js` 로 save/restore 를 `t.after()` 강제.
  ⚠ 분할하면 지금 우연히 성립하던 순서 의존이 드러나 일부가 깨질 수 있다(그 자체가 근거다).
- **초대형 함수 2개** (L, MED). `propertyService.js:539` `getAIRecommendations` ≈ **1,000줄**
  (파일 1,600줄의 63%) · `report.js:1219` `fetchCandidateApts` ≈ **710줄**.
  근거는 "크다" 가 아니라 **실제 편집 사고**다: 직전 51커밋 중 **9개**가 이 두 함수를 고쳤고
  그중 3개가 "직전 커밋이 앵커 불일치로 **절반만 반영**됐다"(`9e65c1a`) ·
  "다른 함수의 같은 매핑 줄이 주입을 통과시켰다"(`7fc05d1`) 처럼 스스로 사고를 자인한다.
  ⚠ 착수 전 **골든 스냅샷**(같은 입력 → 같은 15곳·같은 순서) 고정이 필수.
  ⚠ `index.html` 분할을 09-05 에 보류한 선례가 있다 — 사용자 0 상태의 대형 리팩터링 판단은 운영자 몫.

## 2026-09-06 **실행** 라운드에서 새로 확인된 후속 (백로그)

계획을 실제로 실행·검증하며 나온 것들이다. 감사 시점엔 없던 관찰이라 따로 적는다.

- **분석 카드의 전세가율이 표본 완전성을 밝히지 않는다** (S, HIGH).
  Plan 041 이 문구를 `${jeonseRate}% (최근 6개월 전세 실거래 기준)` 로 바꾸며 **"6개월" 이라는
  사실 주장을 새로 추가**했다. 그런데 Plan 037 이 밝혔듯 일부 달이 실패하면 표본은 6개월보다 얇다.
  · 보고서 경로는 이미 `표본 n/6개월` 로 밝힌다 · **분석 카드는 밝히지 않는다** — 같은 데이터인데 한쪽만 정직하다.
  · 재료는 있다: `getJeonseByApt` 가 `monthsFailed`·`monthsTotal` 을 배열 속성으로 싣고,
    `backend/services/analysisService.js:510` 이 그 반환값을 그대로 받는다.
  → 보고서와 같은 방식으로 표본 개월을 카드 문구에 반영할 것. 041 범위 밖이라 손대지 않았다.

- **`summarizeMarketSignal` 의 점수 가산(2/1/0)에 가드가 없다** (S, MED).
  리뷰어 주입 실측: `volMap.up` 의 `score: 2` 를 `3` 으로 바꿔도 **전 테스트가 초록**이었다.
  Plan 041 이 문구만 다루기로 한 결과이지 041 의 결함은 아니다. 다만 이 산식은 `buySignal`·
  `reliability` 로 화면에 노출되므로 값이 조용히 바뀌면 아무도 모른다.
  → `_internals.calcBuySignal` 이 이미 export 돼 있으니 입력→출력 실행 테스트로 고정할 수 있다.

- **`/share` 에 레이트리밋이 없다** (S, LOW — 증폭이 막힌 뒤로는 우선순위가 낮다).
  Plan 035 로 비용이 정적 응답 수준으로 돌아와 긴급하지 않다. 다만 `generalLimiter` 는 `/api/` 에만
  걸려 있고(`backend/server.js:213`) `/share` 는 무인증·무리밋 공개 경로다.
  ⚠ 조이면 카카오톡·X 미리보기 크롤러가 카드를 못 받는다 — 크롤러 UA 를 고려한 상한이 필요하다.

- **전월세 공유 캐시에 이미 심긴 빈 배열** (XS, 자연 해소 예상).
  Plan 037 이 새 오염은 막았지만 수정 이전에 Redis 에 들어간 빈 항목은 요청 경로가 아직 히트로 받는다.
  `isRentCached` 가 빈 값을 거부하게 됐으므로 **예열 cron 이 다음 회차에 덮어쓴다**(하루 내).
  → 배포 하루 뒤 런타임 로그에서 `전월세` 를 검색해 표본 결손이 사라졌는지 확인만 하면 된다.

- **브랜치 보호가 직접 푸시로 우회된다** (운영자 판단 사항).
  2026-09-06 푸시 시 원격이 `Bypassed rule violations for refs/heads/master: 3 of 3 required status
  checks are expected` 를 반환했다. CI 는 사후에 초록으로 확인했지만 **게이트가 실제로 막지는 않았다.**
  PR 경유로 바꿀지, 직접 푸시 + 사후 확인을 유지할지는 운영자 결정이다.

### 실행 라운드에서 **기각**한 것 (재조사 방지)

- **브리핑 재시도 마커의 앞/뒤 순서를 테스트로 고정하기**: **불가능하고 불필요하다.**
  리뷰어가 마커를 재계산 뒤로 옮기는 주입을 했는데 잡히지 않아 테스트 공백으로 의심했으나,
  `buildBriefingPayload` 전문을 읽어 확인한 결과 **모든 `await` 이 각자 try/catch 로 감싸여 있고**
  마지막은 객체 리터럴 구성뿐이라 **예외를 밖으로 던질 수 없다**. 두 순서는 오늘 관측 차이가 0 이다.
  앞에 두는 것은 그 전제가 깨질 때를 대비한 방어일 뿐이며, 구별할 동작이 없으니 테스트도 쓸 수 없다.

## 의존성 정책 결정 — express 4 유지 (Plan 042, 2026-09-06)

계획 042 의 실제 산출물이다. **설정 변경은 하지 못했고, 그 이유가 이 기록의 절반이다.**

### 실측 (2026-09-06, `npm audit --omit=dev`)

**moderate 5건** — 구성은 다음과 같다:

| 패키지 | 취약점 | 유입 경로 |
|---|---|---|
| `fflate` 0.7.0–0.7.4 | unzipSync 무한 루프(악성 ZIP64) | `satori@0.33.4` → `fflate@0.7.3` |
| `qs` 2.2.5–6.15.3 | array-limit bypass · isBuffer DoS (2건) | `express@4.22.2` |
| `body-parser` 1.20.5–1.20.6 | 위 qs 연쇄 | `express` |
| `express` 4.22.2 | 위 두 개 연쇄 | 직접 의존 |

⚠ **감사 원본 기재를 정정한다.** 2026-09-06 감사에서 이 5건을 "qs/body-parser/express" 로만 적었는데
`fflate` 1건이 빠져 있었다(측정 시 grep 이 그 세 이름만 봤다). 개수 5는 맞고 **구성이 틀렸다.**

**`fflate` 도달 가능성 — 낮음(코드 확인).** 이 advisory 는 `unzipSync` 가 **공격자가 준 ZIP 아카이브**를
파싱할 때 무한 루프에 빠지는 것이다. 우리 OG 렌더 경로는 **저장소에 벤더링한 폰트 파일만** 읽고
(`backend/services/ogImageService.js`), 사용자 입력에서 온 아카이브를 satori 에 넘기지 않는다.
npm 이 제시하는 해결은 `satori@0.32.0` 으로의 **다운그레이드**인데, 0.33 은 하드 요구사항이었다
(0.10→0.33 승격은 이미 프로덕션 사고를 거쳐 안착했다 — `1890804`). 되돌리지 않는다.

### 왜 dependabot 설정을 바꾸지 못했나 (공식 문서 확인)

목표는 "express 5 PR 이 **보이게** 만드는 것" 이었다. 세 방법을 전부 검토했고 전부 막혔다:

1. **같은 ecosystem 에 블록 2개** (계획서 원안) — ❌ 불가.
   GitHub 공식 문서: *"If you need to use more than one block in the configuration file to define
   updates for a single target branch of an ecosystem, you must ensure that all values are unique
   and there is no overlap in directories defined."*
   현재 블록이 `directories: ["/", "/backend"]` 라 두 번째 블록은 반드시 겹친다.
2. **와일드카드 `ignore` 에 express 예외** — ❌ 불가. 부정(negation) 문법이 없다.
   `allow` 로 되살리는 것도 안 된다: *"If a dependency is matched by an `allow` and an `ignore`
   statement, then it is ignored."*
3. **디렉터리를 분리해 블록 2개** — ❌ **이 저장소가 이미 겪고 되돌린 방식이다.**
   `.github/dependabot.yml` 의 `DEPENDABOT-EXP-2026-05-10` 주석: 블록을 `/` 와 `/backend` 로
   나눴더니 root PR + backend PR 이 따로 생성돼 한쪽만 적용될 때 `scripts/check-deps-sync.js` 의
   versionMismatch 로 **CI 가 막혔다**(실제 사례 PR #43 + #28). 되돌릴 이유가 없다.

**남은 유일한 수단**은 와일드카드를 **express 를 뺀 나머지 15개 패키지 명시 목록**으로 바꾸는 것인데,
그러면 `package.json` 과 드리프트하는 새 목록이 생긴다(이 저장소가 반복해서 당한 실패 유형이고,
그 목록을 지켜 줄 게이트도 없다). **채택하지 않는다.**

### 결정 기록 (계획 042 의 산출물)

- **결정**: **express 4 를 유지한다.**
  근거 — moderate 5건의 유일한 해결이 breaking major 이고, blast radius 는 `backend/routes/` 32파일
  + `server.js` + `api/` 진입점이다. 사용자 0 상태에서 그 위험을 감수할 이득이 없다.
- **수용한 위험**: `qs` array-limit bypass · `qs` isBuffer DoS(둘 다 moderate, 모든 쿼리스트링·
  `express.json()` 요청이 도달 경로) · `fflate` unzipSync 무한 루프(위 근거로 **도달 불가 판단**).
- **재검토 시점** (셋 중 **먼저 오는 것**):
  (a) 심각도가 **high 이상으로 상향** · (b) express 4 **EOL 공지** · (c) **결제 오픈으로 실사용자 진입**.
- **관측 방법**: 릴리스 전 `npm audit --omit=dev` 육안 확인.
  CI 의 차단 게이트는 `--audit-level=high` 라 moderate 는 신호를 남기지 않는다 — **그래서 이 기록이 필요하다.**
  ⚠ dependabot 이 express 5 PR 을 **영영 만들지 않는다**는 사실이 이 결정의 전제다.
  즉 "언젠가 PR 이 오면 그때 보자" 는 성립하지 않는다. 위 세 트리거를 사람이 봐야 한다.

## Findings considered and rejected (재감사 방지)

2026-08-09 라운드:

- **report.js 1,358줄 서비스 분리**: 실이득 대비 이관 리스크(클로저 컨텍스트) 큼 — 순수 함수
  테스트(백로그)부터 선행하는 게 순서. 보류.
- **cache 전역 single-flight**: 40+ 호출부 공유 유틸이라 범위 과대 — 핫스팟 국소 적용만 검토(백로그).
- **청약 피드 ↔ 북마크 지역 연동**: odcloud 지역 필드가 시군구 미제공이라 주소 문자열 매칭 필요 —
  이 레포가 반복 겪은 동명 오배치 함정과 동일 계열, L급. 비권장.
- ~~**Sentry v8→v10**~~ — **2026-08-16 정정: 이미 완료됐다.** `@sentry/node`가 `^10.69.0`
  (루트·backend 양쪽 package.json 실측). 이 항목은 stale 이므로 백로그에서 제거한다.
- **Kakao 호출부 통합·페이징 유틸**: 유효한 부채나 M급 리팩터라 운영자 선택 시 후속 계획으로.
- **PERF-01 report in-flight dedup**: 유효하나 현재 트래픽에서 실발생 빈도 낮음(로그인+크레딧
  게이트 뒤) — 트래픽 성장 시 재평가.
- **PERF-03 report jeonse 병렬화**: 유효(S급)나 사용자 체감 낮음 — 백로그.
  **2026-08-16 재보고됐으나 기각 유지**(같은 근거).
- **관심단지 인앱 피드 / 상세모달 객관정보 노출 (direction)**: 운영자 방향 결정 대기.

2026-08-17 라운드(`/improve`) 추가:

- **`/search/in-bounds`·`/facility` 응답 캐시 도입**: **기각.** 지도 bounds 는 연속값이라 격자 키
  없이는 히트율이 사실상 0이고, 격자를 쓰면 마커 누락/과다라는 정확도 리스크가 생긴다.
  트래픽 히트율 실측 없이 넣으면 메모리만 쓰고 이득이 없다. 도입한다면 **먼저 히트율을 측정**할 것.
- **`pushNotify` 순차 발송·500행 캡·워터마크 일괄 갱신**: 구조는 사실이나 **구독자 실측 3명**
  (push 2 · kakao 1). 지금은 무해 → P3 예방으로 보류. 단 500 캡은 이 레포에서 6회 재발한
  "1000행 조용한 절단" 클래스이므로 구독자가 수백 명대가 되면 **반드시** 먼저 처리할 것.
- **`CLAUDE.md` 의 NCP 지도 Client ID 평문 기재**: **by-design.** README·`.env.example` 이
  "public — 브라우저 노출 전제" 로 명시한 클라이언트 식별자다(서버 시크릿 아님). 다만 NCP 콘솔의
  도메인/리퍼러 제한 여부는 코드로 확인 불가 — 운영자 확인 사항으로만 남긴다.
- **`check-deps-sync.js` 가 transitive drift 를 못 잡음**: 지적은 사실(manifest range 만 비교).
  다만 두 lockfile 의 공통 224개 패키지를 실제 대조한 결과 **현재 diff 0건**. npm workspaces 전환은
  Vercel 번들 전제와 상호작용해 L급 → 백로그(필요해지면 스파이크부터).

2026-09-06 라운드(`/improve`) 추가:

- **`drizzle-orm` 런타임 제거**: **기각(3회째 재제안).** 이번 라운드에서 서브에이전트 둘이
  각도만 달리해 다시 올렸다. 그중 하나의 핵심 근거("`db/schema.js` 를 require 하는 코드가
  저장소에 0건")는 **사실이 아니다** — `backend/db/client.js:18` 이 `require('./schema')` 로
  실제 require 하고 그 사슬이 `drizzle-orm/pg-core` 를 로드한다(상위 세션 직접 확인).
  정확한 서술은 "require 는 실행되고, 그 산물인 재export(`client.js:109`)만 소비처 0" 이다.
  `b7b0ce4` 에서 이미 시도·철회됐다. **재제안 금지.**
  (다만 이번에 새로 확인된 사실 2개는 기록해 둔다: ① `scripts/check-deps-sync.js` 가
  실행되지 않는 설계용 파일까지 walk 해 이 의존성을 루트 deps 에 강제한다 ②
  `backend/drizzle.config.js` 의 `out: './db/migrations'` 디렉터리가 존재하지 않는다.
  둘 다 가치가 낮아 계획화하지 않는다.)
- **`CLAUDE.md` 의 지도 SDK 클라이언트 ID 평문 기재**: **by-design**(2026-08-17 판정 유지).
  브라우저 노출 전제의 public 식별자다. 재보고 금지. NCP 콘솔의 리퍼러 제한 여부만 운영자 확인 사항.
- **정적 경로 보안 헤더 부재**: 2026-09-02 에 `vercel.json headers` 로 **해결됨**. stale 지적.

2026-08-16 라운드 추가:

- **`search.js` 의 apt_master 쿼리에 abort 상한 부재**: 구조적으로는 비일관이나 실측상 해당
  쿼리는 4.76ms·23ms 로 이미 충분히 빠르다. `master-*-partial` 강등 카운터가 늘어나는 정황이
  관측되면 그때 재검토.
- **`geocodeCacheService` 의 Kakao 쿼터 리셋이 UTC 자정 기준**: `dailyLimit.js` 의 KST 자정
  수정과 같은 클래스지만, 사용자 노출 한도가 아니라 내부 경고 임계치(60K/100K)라 영향이 낮다.
- **`getDataBasis()` Redis 2차 캐시 누락**: 계획 009(인덱스)가 들어가면 이 조회가 2.2초 → 1ms
  미만이 되므로 **Redis 캐시의 이득이 거의 사라진다.** 009 이후 재평가.

## 감사 범위 밖(미감사) 고지

2026-08-16 라운드는 이전에 미감사로 남았던 영역을 우선 다뤘다(프론트 전체 XSS 싱크·Supabase
RLS 정책 전문·워크플로). 그럼에도 아래는 여전히 미검증이다:

- ~~**Supabase 마이그레이션의 실적용 여부**~~ → **계획 019 에서 종결(2026-08-16).**
  27개 파일 선언 vs `pg_catalog` 실측을 전수 대조했고, **양방향으로 어긋난 것**을 찾았다
  (overlap GIST 제약은 3개월+ 미적용 / `tg_set_updated_at` 은 파일 없이 존재). 미적용분은 적용 완료.
  ⚠ 이 항목이 "미검증"으로 남아 있던 것은 **문서 정합 오류**였다(감사 #51 지적) — 같은 문서 안에서
  019 는 종결을 적고 여기는 미검증을 적고 있었다.
- **결제 실거래 경로**: `TOSS_*` 키 미설정 상태라 런타임 검증 불가 — 코드 리딩만 수행.
- **모바일 실기기 동작**, **프론트 런타임 성능 프로파일링**.

2026-09-06 라운드에서 **보지 않은 것**(범위를 좁힌 결과 — 재감사 시 여기서 시작):

- **`285ef31` 이전 코드 전부.** 2026-09-02 전면 감사 + 09-05 재대조가 훑었다는 전제로 제외했다.
  그 전제가 의심되면 이 라운드가 아니라 그 감사를 재검증할 것.
- **프로덕션 DB 실측 0.** 이번 라운드는 코드·파일 대조만 했다. 그래서 `supabase/schema.sql`
  드리프트도 "파일 간 모순" 으로만 확정했고 **어느 쪽이 프로덕션인지는 확인하지 못했다**
  (마이그레이션 헤더의 자기 진술을 근거로 삼았다). 이 저장소의 확립된 원칙대로
  `pg_catalog` 직접 조회로만 확정할 수 있다.
- **결제 실거래 경로** — `TOSS_*` 키 미설정이라 여전히 런타임 검증 불가.
- **프론트 런타임 성능·모바일 실기기** — 이전 라운드와 동일하게 미검증.
- **성능 발견의 실제 크기.** PERF 계열(추천 팬아웃 등)은 **구조**를 확정했을 뿐
  엔드포인트 실측을 하지 않았다. 이 저장소는 `EXPLAIN` 시간을 매 요청 비용으로 오해해
  없는 병목을 고칠 뻔한 이력이 있으므로, 착수 전 반드시 **사용자가 타는 분기별로** 실측할 것.
- 정확성 감사에서 제기된 두 건의 **실발생 이력**(Sentry/로그): 코드상 위험 메커니즘만 확인했고
  프로덕션에서 실제로 발생했는지는 확인하지 못했다(계획 011 의 A·B 항목).
