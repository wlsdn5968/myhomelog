# 083 — 랜딩의 "5Y 가격 추세 · 단지 선택 시 실 데이터 차트" 문구를 사실에 맞춘다

**작성 기준 커밋**: `2c8d13b` (2026-09-16) · 우선순위 P2 · 작업량 XS · 의존: 없음

## 전제 확인 (계획자가 라이브·DB·코드로 확인한 것)
- 랜딩 카드 `frontend/index.html:2956~2974`: 라벨 "참고 · 5Y 가격 추세 시각화", 가짜 축(14억/10억/6억 · 2021~2025), 하단 "단지 선택 시 실 데이터 차트". SVG 는 하드코딩된 예시 곡선(`aria-label="5년 가격 추이 차트"`).
- **DB 실측**: `molit_transactions` 의 `deal_date` 범위 **2025-05-01 ~ 2026-09-14**(468,303건). 5년 추세를 그릴 데이터가 없다.
- **라이브 실측**: 단지 상세 모달(`#DM`) 안의 `canvas`·`svg` 는 **0개** — 단지를 선택해도 차트는 나오지 않는다(실거래가 탭은 표, 가격 시그널 탭의 월별 거래량은 CSS 막대).
- 절대 룰 ②(정확한 출처·환각 차단)에 걸리는 **약속 위반**: 없는 데이터·없는 기능을 첫 화면이 약속한다.

## 범위
- 수정: 위 카드의 **텍스트·aria-label 만**. SVG 도형·레이아웃·CSS 는 그대로(디자인 시안 코드화는 별도 결정 사항).

## Step 1 — 문구 교체 (정확히 이 4곳)
| 현재 | 변경 |
|---|---|
| `참고 · 5Y 가격 추세 시각화` | `예시 · 가격 흐름 시각화(실제 데이터 아님)` |
| `aria-label="5년 가격 추이 차트"` | `aria-label="가격 흐름 예시 그래프 — 실제 데이터 아님"` |
| 축 라벨 `2021` `2022` `2023` `2024` `2025` (5개 `<text class="lv-chart-axis" …>`) | 그대로 두되 **연도 숫자를 제거**해 빈 축으로(예시임을 분명히) — 각 `<text>` 의 내용만 비운다 |
| `단지 선택 시 실 데이터 차트` | `단지 선택 시 2025.05 이후 실거래 표·가격 위치·월별 거래량 (국토부 신고 기준)` |
`2025.05` 는 하드코딩하지 말고 가능하면 `window._dataCounts`/health 의 최초 적재월을 쓰되, 그 값이 이 화면에서 접근 불가하면 하드코딩하고 주석에 `DATA-RANGE-2026-09-16: molit_transactions min(deal_date)=2025-05-01 실측` 을 남긴다.

## 검증·완료 기준
- `grep -c "5Y 가격 추세\|5년 가격 추이\|실 데이터 차트" frontend/index.html` → **0**.
- `npm run verify` → `fail 0`(382). 프론트 계약 테스트가 위 문구를 단언하면(`grep -rn "5Y\|실 데이터 차트" backend/test/`) 그 기대치를 새 문구로 갱신하고 보고.
- 커밋 1개: `fix(랜딩): 없는 5년 추세 차트 약속 제거 — 실제 적재 범위(2025.05~) 명시 (Plan 083)`.

## STOP 조건
- 같은 문구가 다른 화면(브리핑·공유 카드 등)에도 있다(`grep` 결과 4곳 외) → 목록을 보고하고 랜딩만 고친다.
